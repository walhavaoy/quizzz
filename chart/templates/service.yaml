apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-quizzz
  labels:
    app: {{ .Release.Name }}-quizzz
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
  selector:
    app: {{ .Release.Name }}-quizzz
